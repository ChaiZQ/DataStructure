# DP问题

## 采用递归方法求解

递归函数编写的步骤

```c
T_ func(...)
{
  /*首先检查输入参数是否符合条件*/
  if(input is valid){
    //检查静态数组是否存储了当前状态
    if(recorded)
    {
      //直接返回记录值
      return (recorded).
    }
  }
  /*然后检查终止条件*/
  if(ending){
    keeping;
    return;
  }
  /*处理一般情况*/
  do something;
}
```

